import React from "react";

import ShowCondition from "../../components/drawer/Drawer";

const ShowConditionContainer = () => {
    return <ShowCondition />;
};

export default React.memo(ShowConditionContainer);
